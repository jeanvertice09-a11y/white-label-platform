{
  "name": "@white-label/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@white-label/audit": "0.1.0",
    "@white-label/payments": "0.1.0"
  }
}
