import { useState } from "react";
import { createMerchantCustomer } from "../../lib/server/operations-customers.functions.ts";
function field(form:FormData,key:string):string{const value=form.get(key);return typeof value==="string"?value:"";}
export function CustomerCreateForm({onCreated}:Readonly<{onCreated?:()=>Promise<void>}>):React.JSX.Element{const[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[success,setSuccess]=useState("");async function submit(event:React.SyntheticEvent<HTMLFormElement>):Promise<void>{event.preventDefault();const formElement=event.currentTarget,form=new FormData(formElement);setBusy(true);setMessage("");setSuccess("");try{await createMerchantCustomer({data:{name:field(form,"name"),phone:field(form,"phone")||null,email:field(form,"email")||null,document:field(form,"document")||null,birthDate:field(form,"birthDate")||null,notes:field(form,"notes")||null}});formElement.reset();await onCreated?.();setSuccess("Cliente cadastrado e lista atualizada.");}catch(error){setMessage(error instanceof Error?error.message:"Não foi possível criar o cliente.");}finally{setBusy(false);}}return <details className="k-composer"><summary><span><strong>Novo cliente</strong><small>Cadastro manual para atendimento e CRM.</small></span><span className="k-composer__action">Cadastrar</span></summary><form className="k-composer__body" onSubmit={(event)=>{void submit(event);}}><div className="k-form__grid"><div className="k-field"><label htmlFor="customer-name">Nome</label><input id="customer-name" name="name" required /></div><div className="k-field"><label htmlFor="customer-phone">Telefone</label><input id="customer-phone" name="phone" inputMode="tel" /></div><div className="k-field"><label htmlFor="customer-email">E-mail</label><input id="customer-email" name="email" type="email" /></div><div className="k-field"><label htmlFor="customer-document">Documento</label><input id="customer-document" name="document" /></div><div className="k-field"><label htmlFor="customer-birth">Nascimento</label><input id="customer-birth" name="birthDate" type="date" /></div><div className="k-field k-field--full"><label htmlFor="customer-notes">Observações</label><textarea id="customer-notes" name="notes" /></div></div>{message?<div className="k-inline-state k-inline-state--error"><span>{message}</span></div>:null}{success?<div className="k-inline-state"><strong>Concluído</strong><span>{success}</span></div>:null}<div className="k-actions"><button className="k-button k-button--primary" disabled={busy} type="submit">{busy?"Salvando…":"Salvar cliente"}</button></div></form></details>;}
