import { describe,expect,test } from "bun:test";
import { buildAuditEntry } from "../../packages/audit/src/sanitizer.ts";
describe("audit sanitizer",()=>{test("remove senha, tokens, secrets, credentials e cookies recursivamente",()=>{const e=buildAuditEntry({actorUserId:"u1",tenantId:"t1",storeId:null,action:"login",resourceType:"session",resourceId:null,metadata:{password:"123",access_token:"abc",refresh_token:"r",api_key:"k",Authorization:"Bearer x",cookie:"c",webhook_secret:"s",credentials:{username:"visible",password:"hidden"},nested:{client_secret:"cs",credential:"raw",ok:1},list:[{token:"x"},{safe:true}]}});const m=e.metadata!;expect(m["password"]).toBe("[REDACTED]");expect(m["access_token"]).toBe("[REDACTED]");expect(m["Authorization"]).toBe("[REDACTED]");expect(m["credentials"]).toBe("[REDACTED]");expect((m["nested"] as Record<string,unknown>)["client_secret"]).toBe("[REDACTED]");expect((m["nested"] as Record<string,unknown>)["credential"]).toBe("[REDACTED]");expect(((m["list"] as Record<string,unknown>[])[0])?.["token"]).toBe("[REDACTED]");expect((m["nested"] as Record<string,unknown>)["ok"]).toBe(1);});});
